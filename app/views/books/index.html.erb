<%= render "home/header" %>
<div class="body">
  <div class="container">
    <% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>
<%= render "books/new"  %>
    <div class="col-xs-9" >
          <h2>Books</h2>
          <table class="table table-hover">
            <thead>
              <tr>
                <th></th>
                <th>Title</th>
                <th>Opinion</th>
                <th callspan="3" ></th>
              </tr>
            </thead>
            <tbody>
              <% @books.each do |book| %>
              <tr>
                <td><%=  %></td>
                <td><%= book.title %></td>
                <td><%= book.body %></td>
              </tr>
              <% end %>
            </tbody>
           </table>
    </div>
</div>
</div>
</div>

<%= render "home/footer" %>
